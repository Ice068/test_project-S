# SRS เว็บสื่อการสอนรายวิชาคอมพิวเตอร์ เรื่อง หน้าเว็บ

---

## 1. บทนำ

### 1.1 วัตถุประสงค์ 

เอกสารนี้จัดทำขึ้นเพื่ออธิบายความต้องการของระบบเว็บสื่อการสอนภาษา HTML 
สำหรับนักเรียนระดับมัธยมศึกษาปีที่ 6 เพื่อใช้เป็นแนวทางในการพัฒนา ทดสอบ และใช้งานระบบ

### 1.2 ขอบเขตของระบบ

ระบบเป็นเว็บไซต์ที่ให้ผู้เรียนสามารถ:

* เรียนรู้เนื้อหา HTML

* ทดลองเขียนโค้ด

* ดูผลลัพธ์แบบ Real-time

* ทำแบบทดสอบและดูคะแนน

---

## 2. ภาพรวมของระบบ (Overall Description)

### 2.1 ลักษณะทั่วไปของระบบ

เป็นระบบ Web Application ใช้งานผ่าน Browser โดยไม่ต้องติดตั้งโปรแกรม

### 2.2 ผู้ใช้งานระบบ (User)

* นักเรียน (ผู้ใช้งานหลัก)

* ผู้สอน (ดูเนื้อหา/ใช้สื่อ)

### 2.3 ข้อจำกัดของระบบ

ระบบมีข้อจำกัดดังนี้:

#### 2.3.1 ข้อจำกัดด้านฐานข้อมูล

* ระบบ ไม่มีการใช้ฐานข้อมูล (Database) สำหรับจัดเก็บข้อมูลผู้ใช้งาน

* ไม่มีระบบสมาชิก (Login / Register)

    * ข้อมูลต่าง ๆ เช่น:

        * คะแนนแบบทดสอบ
        
        * โค้ดที่ผู้ใช้พิมพ์ใน Code Editor

        * ความคืบหน้าการเรียน

        * จะไม่ถูกบันทึกถาวร (No persistent storage)

#### 2.3.2 การจัดเก็บข้อมูลชั่วคราว

* ข้อมูลอาจถูกเก็บชั่วคราวในหน่วยความจำ (Memory) หรือ Browser (เช่น session)
    
    * เมื่อผู้ใช้:
        
        * รีเฟรชหน้าเว็บ
        
        * ปิด Browser

#### 2.3.3 ข้อจำกัดด้านฟังก์ชัน

* ไม่สามารถบันทึกประวัติการเรียน

* ไม่สามารถติดตามผู้ใช้งานรายบุคคล

* ไม่สามารถจัดเก็บคะแนนระยะยาว

#### 2.3.4 เหตุผลของข้อจำกัด

* เพื่อลดความซับซ้อนของระบบ

* เพื่อโฟกัสที่การเรียนรู้ HTML เบื้องต้น

* เพื่อให้พัฒนาเสร็จภายในเวลาที่กำหนด

---

## 3. Functional Requirements (ความต้องการเชิงฟังก์ชัน)

### 3.1 ระบบบทเรียน

* ผู้ใช้สามารถเลือกบทเรียนได้

* ระบบแสดงเนื้อหา HTML เช่น:

    - โครงสร้าง HTML
    - Text
    - Link
    - Image
    - Table

### 3.2 ระบบ Code Editor

ระบบต้องมีพื้นที่สำหรับให้ผู้ใช้ทดลองเขียนโค้ด HTML และแสดงผลลัพธ์แบบ Real-time โดยมีรายละเอียดดังนี้:

#### 3.2.1 ความสามารถพื้นฐาน

- ผู้ใช้สามารถพิมพ์โค้ด HTML ได้

- ระบบแสดงผลลัพธ์ทันที (Real-time Preview)

- รองรับการแก้ไขโค้ดแบบทันที

#### 3.2.2 ระบบจัดการ Error

ระบบต้องสามารถจัดการข้อผิดพลาดของโค้ด HTML ได้ ดังนี้:

เมื่อผู้ใช้พิมพ์โค้ดผิด เช่น:

* ลืมปิด tag (<p> ไม่ปิด)

* เขียน tag ไม่ถูกต้อง (<imgg>)

* ระบบ ไม่ทำให้หน้าเว็บหลักพัง (ไม่กระทบระบบหลัก)

#### 3.2.3 รูปแบบการแสดงผลเมื่อเกิด Error

ระบบจะมีพฤติกรรมดังนี้:

- กรณีที่ 1: HTML แสดงผลได้ (Browser tolerant)
    
    * ระบบแสดงผลตามจริงของ Browser
    
    * แม้โค้ดจะไม่สมบูรณ์

- กรณีที่ 2: โครงสร้างผิดพลาดชัดเจน
    
    * ระบบสามารถแสดงข้อความแจ้งเตือน เช่น:
        
        * “Tag ไม่สมบูรณ์”
        
        * “Syntax ผิดพลาด”
        
        * หรือ highlight จุดผิดในโค้ด

#### 3.2.4 ความปลอดภัยของระบบ (Sandbox)

* ระบบ Preview ต้องแยกจากหน้าเว็บหลัก (Sandbox เช่น iframe)

* ป้องกันไม่ให้โค้ดของผู้ใช้กระทบ:

* โครงสร้างเว็บไซต์หลัก

* ข้อมูลระบบ

#### 3.2.5 คุณสมบัติเพิ่มเติม (Optional)

* แสดงคำแนะนำเมื่อเขียนผิด

* Auto-complete tag HTML

* ปุ่ม Reset โค้ด

### 3.3 ระบบแบบทดสอบ

ระบบต้องรองรับแบบทดสอบเพื่อประเมินความเข้าใจของผู้เรียน โดยแบ่งออกเป็น 2 รูปแบบ ดังนี้:

#### 3.3.1 แบบทดสอบแบบปรนัย (Multiple Choice)

- ผู้ใช้สามารถเลือกคำตอบจากตัวเลือกที่กำหนด

- ระบบตรวจคำตอบอัตโนมัติ

- แสดงผลคะแนนหลังทำแบบทดสอบเสร็จ

#### 3.3.2 แบบทดสอบแบบเติมคำ (Fill in the Blank)

- ผู้ใช้สามารถกรอกคำตอบลงในช่องว่าง

- ใช้สำหรับทดสอบความเข้าใจคำสั่ง HTML เช่น:

- เติม tag เช่น <p>, <a>, <img>

- เติม attribute เช่น href, src

- ระบบต้องตรวจคำตอบอัตโนมัติ

- แสดงผลว่าถูก/ผิดทันที (สามารถใช้ระหว่างเรียนได้)

#### 3.3.3 คุณสมบัติเพิ่มเติม

- รองรับการทำแบบทดสอบระหว่างเรียน (In-lesson quiz)

- ผู้ใช้สามารถทำซ้ำได้

- ระบบสามารถแสดงเฉลยหรือคำอธิบายเพิ่มเติม



### 3.4 ระบบนำทาง (Navigation)

* ผู้ใช้สามารถเลือกเมนูได้ เช่น:

* หน้าเรียน

* ทดลองโค้ด

* แบบทดสอบ

---
### ตาราง

| ID        | Requirement Name            | Description |
|-----------|----------------------------|-------------|
| FR-01     | แสดงบทเรียน                | ระบบต้องสามารถแสดงเนื้อหา HTML เช่น โครงสร้าง, Text, Link, Image, Table |
| FR-02     | เลือกบทเรียน               | ผู้ใช้สามารถเลือกหัวข้อบทเรียนได้ |
| FR-03     | นำทางบทเรียน              | มีปุ่ม ถัดไป (Next) และ ย้อนกลับ (Previous) |
| FR-04     | ภาษาไทย                    | เนื้อหาและ UI ทั้งหมดต้องแสดงเป็นภาษาไทย |
| FR-05     | Code Editor                | มีพื้นที่ให้ผู้ใช้พิมพ์โค้ด HTML |
| FR-06     | Real-time Preview          | แสดงผลลัพธ์ทันทีหลังพิมพ์โค้ด |
| FR-07     | Syntax Highlighting        | ไฮไลท์สีโค้ด HTML (tag, attribute, value) |
| FR-08     | Error Handling             | แจ้งเตือนเมื่อโค้ด HTML ผิด |
| FR-09     | Sandbox Preview            | แสดงผลใน iframe แยกจากระบบหลัก |
| FR-10     | Sanitization               | กรองโค้ดอันตราย เช่น <script> |
| FR-11     | แบบทดสอบปรนัย             | รองรับ Multiple Choice |
| FR-12     | แบบทดสอบเติมคำ            | รองรับ Fill in the Blank |
| FR-13     | ตรวจคำตอบอัตโนมัติ        | ระบบตรวจคำตอบและให้ผลลัพธ์ |
| FR-14     | แสดงคะแนน                 | แสดงคะแนนหลังทำแบบทดสอบ |
| FR-15     | แสดงเฉลย                  | แสดงคำตอบที่ถูกต้อง |
| FR-16     | ทำแบบทดสอบซ้ำ             | ผู้ใช้สามารถทำซ้ำได้ไม่จำกัด |
| FR-17     | Responsive Design          | รองรับมือถือ แท็บเล็ต และคอมพิวเตอร์ |
| FR-18     | Mobile Friendly UI         | ปุ่มและ UI ใช้งานง่ายบนมือถือ |
| FR-19     | จำกัด Code Editor         | Code Editor ใช้งานได้ดีบนหน้าจอใหญ่ |

--- 

## 4. Non-Functional Requirements (ความต้องการไม่ใช่ฟังก์ชัน)

### 4.1 Usability

* ใช้งานง่าย (User-friendly)

* รองรับผู้เริ่มต้น

### 4.2 Performance

* แสดงผลโค้ดแบบ Real-time

* โหลดหน้าเว็บรวดเร็ว

### 4.3 Compatibility

* รองรับ Browser เช่น Chrome, Edge

### 4.4 Security

ระบบต้องมีมาตรการด้านความปลอดภัย เพื่อป้องกันการใช้งานที่อาจก่อให้เกิดอันตราย ดังนี้:

#### 4.4.1 การป้องกันการโจมตีจากโค้ดผู้ใช้

- เนื่องจากระบบมี Code Editor ที่อนุญาตให้ผู้ใช้พิมพ์โค้ด HTML และแสดงผลทันที
จึงมีความเสี่ยงต่อการฝังโค้ดอันตราย เช่น <script> (Cross-Site Scripting: XSS)

#### 4.4.2 การทำ Sanitization

ระบบต้องทำ Sanitization กับโค้ดที่ผู้ใช้ป้อนเข้ามา

กรอง/ลบ tag หรือคำสั่งที่อันตราย เช่น:

- <script> onload, onclick

- อนุญาตเฉพาะ HTML ที่ปลอดภัย (Safe HTML)


#### 4.4.3 การจำกัดการทำงานของโค้ด (Sandbox)

* การแสดงผล Preview ต้องอยู่ในสภาพแวดล้อมแยก (Sandbox เช่น iframe)

* ป้องกันไม่ให้โค้ดของผู้ใช้:

* เข้าถึง DOM ของเว็บหลัก

* แก้ไขระบบ

* รัน JavaScript ที่เป็นอันตราย

#### 4.4.4 การแสดงผลอย่างปลอดภัย

* ระบบต้อง render เฉพาะ HTML ที่ผ่านการตรวจสอบแล้ว

* หากพบโค้ดไม่ปลอดภัย:

* ไม่แสดงผลส่วนนั้น

* หรือแจ้งเตือนผู้ใช้

#### 4.4.5 ข้อจำกัดด้านความปลอดภัย

* ไม่รองรับการรัน JavaScript จากผู้ใช้

* รองรับเฉพาะ HTML เบื้องต้นเท่านั้น

---

## 5. Assumptions

* ผู้ใช้มีพื้นฐานคอมพิวเตอร์เบื้องต้น

* มีอินเทอร์เน็ตใช้งาน
